# ── Python ──────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# ── Virtual envs ────────────────────────────────────────
.venv/
venv/
env/
ENV/

# ── OS / editor cruft ───────────────────────────────────
.DS_Store
*.swp
*~

# ── Downloadable corpora / large data (regenerated by the loaders) ──
# tiny-shakespeare is fetched from the karpathy URL by seq/charlm.py;
# text8 is downloaded to $PRISM_RESULTS at runtime. Keep them out of git.
seq/data/shakespeare.txt
seq/data/text8
**/text8
*.zip
*.gz

# ── Model checkpoints (not produced by this repo, but guard anyway) ──
*.pt
*.ckpt
*.safetensors
