# Prizma runtime dependencies.
# For CPU-only environments (e.g. CI), install torch from the CPU index first:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
numpy>=1.24
torch>=2.2
matplotlib>=3.7
